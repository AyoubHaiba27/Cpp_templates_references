///String functions :

#include<string>//include sstring library

string s="hello";

getline(cin,s); //reads all chars until enter is pressed
getline(cin,s,'@'); //reads all chars until @ appears
s.size(); //returns size of string int
s.begin();s.end; //returns position of the first or last element in string
s[i]; //allows access to a sepecific element in the string
s.clear(); //clears all the elements in the string
s.pop_back(); //delets the last element in the string
s.push_back(x); //adds char x to the end of the string
s=s+"xyz" //same as push_back() function but can add many chars not only one
s="xyz"+s; //attached xyz to the beginning of the string
s=s+d; //attaches string d to string s;
s.erase(pos,cnt); //erases elements starting from pos and counts cnt
s.erase(pos); //erases from pos to the end of the string
s.find("xyz"); //returns position or (npos) -1
s.substr(pos,cnt); //returns the substring from pos and cnt elements after
sort(s.begin(),s.end()); //sort all of the string
reverse(s.begin(),s.end()); //reverse all of the string
sort(s.begin()+l , s.begin()+r+1) //sort string from l to r
reverse(s.begin()+l , s.begin()+r+1) //reverse string from l to r
s.find_first_of("xyz"); //Finds the first char that occurs from xyz
s.find_last_of("xyz"); //Finds the last char that occurs from xyz
s.find_first_not_of("helo"); //Finds the first occurrence of a character not in the given set
s.find_last_not_of("helo"); //Finds the last occurrence of a character not in the given set
s.replace(0, 5, "hi");  // replaces from index 0 and 5 inecies after
s1.swap(s2);  // s1 swaps with s2
s.empty() //Checks if the string is empty
s.resize(3); //Resizes the string to a specified length
double num = stod(numStr); //Converts a string to a double , num becomes 123.45
int num = stoi(numStr); //Converts a string to an int , num becomes 123
regex() //pattern matching 
